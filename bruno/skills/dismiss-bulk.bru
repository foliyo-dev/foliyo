meta {
  name: Dismiss skills bulk
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/skills/dismiss-bulk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "ids": ["{{skillId1}}", "{{skillId2}}"]
  }
}
